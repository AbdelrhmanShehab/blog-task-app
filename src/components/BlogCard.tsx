import React from "react";

interface BlogCardProps {
  title: string;
  author: string;
  date: string;
  excerpt: string;
  onClick?: () => void;
}

export default function BlogCard() {
  return (<article>
    
  </article>);
}
